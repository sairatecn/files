# 与指定数字相同的数的个数

## 题目信息

- 时间限制：C/C++ 1000MS，其他语言 2000MS
- 内存限制：C/C++ 256MB，其他语言 512MB

## 题目描述

给定一个长度为 $N$ 的整数序列，统计其中与指定数字 $m$ 相同的元素个数。

## 输入描述

输入共两行：

- 第一行包含两个整数 $N$ 和 $m$ ，表示整数序列的长度（ $N≤100N \le 100N≤100$ ）和指定的数字，中间用一个空格分隔；
- 第二行包含 $N$ 个整数，整数之间以一个空格分隔。

## 输出描述

输出一行，表示序列中与 $m$ 相同的数的个数。

## 示例

### 用例输入 1

```text
3 2
2 3 2
```

### 用例输出 1

```text
2
```

---

## 题解

### 解题思路

给定序列长度 $N$ 和目标值 $m$，遍历序列统计等于 $m$ 的元素个数即可。

### 算法分析

1. 读入 $N$ 和 $m$。
2. 定义 `cnt = 0`，循环 $N$ 次读入元素，若等于 $m$ 则 `cnt++`。
3. 输出 `cnt`。

### 参考代码（C++）

```cpp
#include <iostream>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int cnt = 0;
    for (int i = 0; i < n; i++) {
        int x;
        cin >> x;
        if (x == m) cnt++;
    }
    cout << cnt << endl;
    return 0;
}
```

### 复杂度分析

- **时间复杂度**：$O(N)$。
- **空间复杂度**：$O(1)$。
