# 求小数的某一位

## 题目信息

- 时间限制：C/C++ 1000MS，其他语言 2000MS
- 内存限制：C/C++ 256MB，其他语言 512MB

## 题目描述

给定三个正整数 $a,b,na, b, na,b,n$ ，求分数 $ab\frac{a}{b}ba​$ 化为小数后，小数点后第 $n$ 位的数字。

## 输入描述

输入三个正整数 $a,b,na, b, na,b,n$ ，用空格隔开，满足 $0<a<b<100,1≤n≤100000 < a < b < 100, 1 \le n \le 100000<a<b<100,1≤n≤10000$ 。

## 输出描述

输出一个数字，为小数点后第 $n$ 位。

## 示例

### 用例输入 1

```text
1 2 1
```

### 用例输出 1

```text
5
```

---

## 题解

### 解题思路

模拟手工除法过程。先对 $a$ 取余得到 $a \bmod b$（整数部分不影响小数位），然后重复 $n$ 次：将余数乘以 10，再除以 $b$ 得到当前小数位，更新余数。第 $n$ 次的商即为所求。

### 算法分析

1. 读入 $a, b, n$。
2. `a = a % b`（去掉整数部分）。
3. 循环 $n$ 次：`a *= 10`，`digit = a / b`，`a = a % b`。
4. 输出 `digit`。

### 参考代码（C++）

```cpp
#include <iostream>
using namespace std;

int main() {
    int a, b, n;
    cin >> a >> b >> n;
    a = a % b;
    int digit = 0;
    for (int i = 0; i < n; i++) {
        a *= 10;
        digit = a / b;
        a = a % b;
    }
    cout << digit << endl;
    return 0;
}
```

### 复杂度分析

- **时间复杂度**：$O(n)$。
- **空间复杂度**：$O(1)$。
